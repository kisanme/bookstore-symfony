{% extends 'base.html.twig' %}

{% block title %}- Cart{% endblock %}

{% block main %}
<div class="container">
    <div class="col-md-12">
        <h1>Cart</h1>
        <div class="table-responsive">
            <table class="table table-striped table-hover">
                <thead>
                    <td>#</td>
                    <td>Name</td>
                    <td>Author</td>
                    <td>Price</td>
                    <td></td>
                </thead>
                <tbody>
                    {% for book in cart_items %}
                    <tr>
                        <td>{{ loop.index }}</td>
                        <td>{{ book.name }}</td>
                        <td>{{ book.author }}</td>
                        <td>{{ book.price }}</td>
                        <td class="pull-right">
                            <a data-url="{{ path('remove_from_cart', { book: book.id }) }}" class="remove-from-cart btn btn-sm btn-info" data-id="{{ book.id }}"><i class="fa fa-shopping-cart"></i>Remove from Cart</a>
                        </td>
                    </tr>
                    {% endfor %}
                </tbody>
                <tfoot>
                    <tr>
                        <td>Total Sum</td>
                        <td></td>
                        <td></td>
                        <td>{{ invoice.TotalPayable }}</td>
                    </tr>
                    {% for discount in invoice.discounts %}
                    <tr>
                        <td>Discounts</td>
                        <td>{{ discount.name }}</td>
                        <td></td>
                        <td>{{ discount.amount }}</td>
                    </tr>
                    {% endfor %}
                    {# <tr>
                        <td>Coupon Codes</td>
                        <td></td>
                        <td class="form-inline"><input class="form-control" placeholder="Enter Coupon Code" type="text"><button class="btn btn-sm btn-primary">Add coupon code</button></td>
                        <td>{{ invoice.CouponCode }}</td>
                    </tr>  #}
                    <tr>
                        <td>Net Total</td>
                        <td></td>
                        <td></td>
                        <td>{{ invoice.TotalPayable }}</td>
                    </tr>
                </tfoot>
            </table>
        </div>
    </div>
</div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('cart-mixin') }}
{% endblock %}
